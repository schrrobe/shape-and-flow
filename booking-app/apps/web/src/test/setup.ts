/**
 * Shared test setup.
 *
 * Empty of stubs on purpose: a component test that needs a global mock is usually a component
 * doing too much. What lives here is environment repair only.
 */
export {};
